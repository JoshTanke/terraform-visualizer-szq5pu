# GitHub OAuth Configuration
# Required for authentication flow with GitHub
# Obtain these values from GitHub OAuth App settings
VITE_GITHUB_CLIENT_ID=your_github_oauth_app_client_id
VITE_GITHUB_REDIRECT_URI=http://localhost:3000/auth/callback

# API Configuration
# Base URL for REST API communication
# Update for different environments (dev/staging/prod)
VITE_API_BASE_URL=http://localhost:3001/api/v1

# WebSocket Configuration
# Endpoint for real-time updates
# Should match API server's WebSocket endpoint
VITE_WEBSOCKET_URL=ws://localhost:3001/ws

# Environment Configuration
# Controls application behavior and optimizations
NODE_ENV=development

# NOTE: This is an example configuration file
# 1. Copy this file to .env for local development
# 2. Never commit actual credentials to version control
# 3. Use secure methods to manage production credentials
# 4. Ensure all variables are properly set before starting the application
# 5. Different values will be needed for staging/production environments
# 6. Implement regular credential rotation in production